// Font.hpp

#ifndef __SDL2_AUDIO_AUDIO_H__
#define __SDL2_AUDIO_AUDIO_H__

/*

// SDL Includes
#include <SDL_mixer.h>

namespace SDL2
{
	namespace Audio
	{		
		int Initialize()
		{
			int code = Mix_init();
			if(code < 0)
				SDL_Log("Failed initialize SDL2_Mixer: %s", Mix_GetError());

			return code;
		}

		void Finalize()
		{
			Mix_Quit();
		}
	}
}

*/

#endif
